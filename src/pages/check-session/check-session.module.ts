import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CheckSessionPage } from './check-session';

@NgModule({
  declarations: [
    CheckSessionPage,
  ],
  imports: [
    IonicPageModule.forChild(CheckSessionPage),
  ],
})
export class CheckSessionPageModule {}
